{
  "name": "playwright-webhook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx playwright install chromium && ls -R /opt/render/.cache/ms-playwright"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  }
}
